{
  "launch": {
    "version": "0.2.0",
    "configurations": [
      {
        "name": "Xdebug for all projects",
        "type": "php",
        "request": "launch",
        "port": 9000,
        "pathMappings": {
          "/shared/httpd/${workspaceFolderBasename}": "${workspaceFolder}"
        }
      }
    ]
  }
}